# 闭环 #51: 验收体系总表文档

- **日期**: 2026-08-03
- **PR**: #222(功能)+ 本文档(归档)
- **背景**: 12 个 accept 脚本(122 项检查)无单一入口文档——dev-up 仅一句「futu 系由 accept-*.sh 覆盖」,FUTU.md/README 无引用;新成员或老板想跑验收无索引,各脚本的前置条件/参数/环境变量散在各自头部注释。

## 改动

新增 `doc/ACCEPTANCE.md` + README 导航链接:

- **分层**: verify.sh 单测(≡ CI test)→ dev-up 16 项冒烟 → 12 个 accept 脚本逐端点 e2e
- **每脚本一览**: 覆盖对象 / 检查数(2-21 项不等)/ 前置与运行方式(参数缺省取 `$FUTU_GATEWAY_URL` / `$FUTU_PROTO_ADDR` / `$WBOT_PG_DSN`)
- **覆盖矩阵**: 子系统 × 面(CLI/HTTP),标注 futu order 刻意无自动脚本(只测 -dry-run 与红线拒绝路径)
- **对账纪律**: ①端点清单 grep 二进制全部 HTTP 面对照 API.md;②CLI 子命令 verify.sh/dev-up/accept 三维核对;③断言 vs 真实数据分支找零覆盖分支(#40-#50 经验的落盘)

## 验证

- 逐脚本核对 usage/前置后落盘(数据与各脚本头部注释一致)
- CI 5/5

## 备注

- **引擎经验**: 「运维沉淀」候选不止于新脚本——验收体系膨胀后需要单一入口索引,否则 dev-up 的「由 accept 覆盖」注释就是唯一的寻路线索(又一个 #48 式盲区: 注释即文档)。
- **候选池**: 仍枯竭。

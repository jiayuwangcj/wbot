# 闭环 #70: README 日志约束声明对齐现状(zerolog 排期指回 ROADMAP)

- **日期**: 2026-08-03
- **PR**: #257(功能)+ 本文档(归档)
- **背景**: AUTO_ADVANCE 对账引擎 #65「声明 vs 实际」——README 项目约束段「日志:`zerolog`」无「后续实现」标记(同段「部署形态/架构/市场/交易接入/Web/外部通知」六行均有),而代码实测:23 处 std log/fmt 调用、go.mod 无 zerolog 依赖。ROADMAP v3「标准化日志(如 zerolog)随本阶段按需收紧」——该约束是排期项而非已落实项。

## 改动

- README.md 日志行:`zerolog`（当前 std log/fmt 输出;结构化日志随 v3 执行路径阶段按需收紧,见 `doc/ROADMAP.md`）——保留约束 + 声明现状 + 指回排期

## 验证

- 代码零 zerolog、go.mod 无依赖、23 处 std log 计数留档;PR #257 CI 5/5 绿

## 备注

- **引擎经验**: 「项目约束」段内带/不带「(后续实现)」标记是有意区分「愿景 vs 当前」——triage 时对该段逐行对照代码,grep 依赖/调用即可定位未落实声明;修复取「保留约束 + 声明现状 + 指回排期」而非弱化约束(排期在 ROADMAP 已有锚点,升级实现 23 处调用点超最小步)。
- **候选池**: 仍枯竭(待老板 7 项)。

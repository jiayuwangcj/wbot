# 闭环 #79: accept 检查数同步 126 → 135

- **日期**: 2026-08-03
- **PR**: #275(功能)+ 本文档(归档)
- **背景**: 数字同步对账——`scripts/accept-*.sh` 脚本数 12 与文档一致,但**检查项数差 9**:两账户脚本 real-env 扩展后 ACCEPTANCE.md 矩阵未同步——accept-account-snapshot 实际 15(矩阵 10)、accept-account-snapshots-api 实际 7(矩阵 4)。

## 改动

- doc/ACCEPTANCE.md:矩阵两行(10→15、4→7)+ 总表「12 个,126 项」→「12 个,135 项」
- README.md:「12 个脚本,126 项检查」→「12 个脚本,135 项检查」

## 验证

- 逐脚本 `grep -c 'check "'` 核对(12 脚本 135 项);全库 126 清零;docs-only CI check-skip 通过

## 备注

- **引擎经验**: 数字同步全库 grep 只抓「声明处」,**脚本扩展(尤其 real-env 分支类增量)后矩阵行数字最容易漏同步**——对账要逐脚本 grep 实际 check 数与矩阵行比对,不能只查汇总数字;「12 个脚本」对了不代表「126 项」对。
- **候选池**: 仍枯竭(待老板 7 项)。

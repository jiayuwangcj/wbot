# /v1/backtests 跨页全局排序 (S-API-results-sort) — 2026-08-02

状态: ✅ 已合并 (PR #127, commit 58c221a)

## 背景
AUTO_ADVANCE 根任务循环 ⑥ 老板长期目标(UI 打磨)连续推进:回测结果表
表头排序(PR #121)只排「当前页」50 条,备注候选「跨页全局排序需 API
层 sort 参数」——本轮落地,前后端联动:表头点击 → 服务端全库重排。

## 改动
1. **store** (internal/backtest/store.go): ListResults 加 sortKey/desc
   参数;`sortExprs` 白名单映射(id/strategy/symbol/created_at 列 +
   equity/total_return/max_drawdown/bars 从 metrics JSONB cast),
   ORDER BY 片段只从白名单字面表达式构建——**非法键静默回退最新优先,
   杜绝 SQL 注入**;`NULLS LAST` 沉底语义与前端 -Infinity 一致。
   导出 ValidSortKey/SortKeyNames;无 sort 参数行为不变(id DESC)。
2. **httpapi** (backtests.go): 解析 sort/order(缺省 asc),非法键/非法
   order → 400 invalid_request 并列出合法键。
3. **web** (app.js): 表头点击 → `loadSorted` 拼 sort/order 重新拉取
   全局排序结果;本地 sortItems 保留为降级兜底;makeTableSorter 暴露
   state(供查询串拼接)。
4. 测试: TestBacktestsListSort(透传/默认 asc/无参兼容/400 注入尝试
   + SortKeyNames 一致性)+ 前端契约更新。

## 验收
- `go test ./... -count=1` 全绿
- dev-up.sh 10/10 smoke(serve + PG + futu 网关)
- 逐端点契约 17/17:真实 DB 排序验证(id asc/desc、7 键白名单通过、
  3 类非法参数 400)+ app.js 跨页重载契约
- CI: test / db-integration / governance / ci-summary 全 pass

## 备注
- 小坑 1:URL 里 `;` 是 Go query 解析的参数分隔符(测试注入案例被截断
  成合法 sort,测试本身期望错),改用 `'`/`--` 注入尝试。
- 小坑 2:BacktestStore 接口签名变化波及 cmd/wbot/main_test.go 的
  serveFakeBacktestStore(需同步实现)。
- 非法 sort 静默回退(store)vs 400(handler):handler 先校验(用户可
  见错误),store 白名单兜底(纵深防御)。

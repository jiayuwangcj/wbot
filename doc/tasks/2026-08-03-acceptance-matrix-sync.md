# 闭环 #71: ACCEPTANCE.md 覆盖矩阵 dev-up 16→19 数字同步

- **日期**: 2026-08-03
- **PR**: #259(功能)+ 本文档(归档)
- **背景**: 纪律② 数字同步对账——#66 dev-up smoke 16→19 项时同步了三处(第 8 行 / README / RELEASE_DAILY),ACCEPTANCE.md 覆盖矩阵 serve 行「dev-up 16」漏改。2026-08-03 对账定位。

## 改动

- doc/ACCEPTANCE.md:36 覆盖矩阵 serve 行「dev-up 16」→「dev-up 19」(26/41 行的 16 为 accept-watchlist 检查数,含义不同,不动)

## 验证

- 全库 grep「dev-up 16」零残留;PR #259 CI 5/5 绿

## 备注

- **引擎经验**: 数字同步类修改(#66 的 16→19)要**全库 grep 该数字的每一处**再对照含义——本次矩阵行漏改说明当初只同步了「主行」未扫「矩阵/表格行」。对账时以「数字 × 上下文」为单位 grep,不只看主文档。
- **候选池**: 仍枯竭(待老板 7 项)。

# 回测 P1:S7 Discord 推送 + ES 训练门(2026-08-14 老板指令:回测继续开发)

## Goal

回测工具链继续推进(老板 2026-08-14 质问「回测为何没有继续开发」):P0 已合入(4a8df36),现在实施不依赖数据量的 P1 项,核心是 **S7 Discord 推送**(老板明确目标:每轮迭代回测报告 Discord 推送给他)与 **P1-1 ES 训练零覆盖快速失败**。

## 任务清单

1. **S7 基础:backtest CLI `-push`**(不依赖数据量,先交付)
   - `wbot backtest -report -push`:Discord embed 摘要推送(5-7 核心字段:标的/窗口/净收益或 DATA_BLOCKED 状态/覆盖率/费用/停止原因),风险状态不截断
   - **幂等**:报告带 report ID,重复执行同 ID 不重复推送(幂等键落库或本地标记)
   - **失败可重试**:推送失败不影响报告生成;重试同 report ID 幂等
   - Discord token 来自 wbot.conf(serve 同款),不带 token 时 stderr 提示
   - 验收:真实 Discord channel smoke(或 webhook 干跑模式),幂等验证(重复跑不重复发)
2. **P1-1:ES -train 零覆盖快速失败**(评审 P1-1,cmd/wbot/backtest_train.go:63)
   - baseOutcome probe 后检查 DataQuality:EffectiveTrades==0(或 coverage==0)→ 报错退出,恢复旧 ErrNoOptionData 快速失败语义
   - 防「0 收益候选在负基线窗口进可推荐列表」
3. (条件性,数据量允许才做)walk-forward ≥3 fold 框架:数据不足时报告如实标注 not_applicable,不伪造

## Constraints

- worktree: `.claude/worktrees/backtest-p1`(分支 fix/backtest-p1,基于主基线 4a8df36)
- 提交前 scripts/verify.sh 全绿(export PATH="$HOME/go/bin:$PATH");提交署名按实际编写模型(gpt-5.6-sol → Co-Authored-By: gpt-5.6-luna <noreply@openai.com>)
- 只改回测工具链(backtest/backtestreport/backtestes/cmd/wbot backtest 相关);**不碰实时 wheel/下单/推送器现有链路**
- Discord 推送复用现有 notify/discord 包,不新建凭证;敏感值只进 ~/.wbot/
- 回测报告 RESEARCH_ONLY 语义不变;DATA_BLOCKED 不写成收益承诺
- 真实验收用现有 2 天 snapshot 数据(HK.00700/US.JD),不要求完整 DTE 周期

## Links

- 裁决书:~/.claude/plans/mutable-nibbling-music.md(S7 定义:幂等推送/失败可重试/真实 channel smoke)
- 评审报告:回测 P0 评审(2026-08-14,reviewer):P1-1 建议 + db-integration E2E 缺口建议
- 任务记录:doc/tasks/2026-08-14-backtest-p0-sol.md(P0 完成,含数据面裁决)
- 推送器现况:#62/#63 已收口(推送失败重试已实现),S7 复用其模式

## State

- [x] S7 -push 幂等推送(报告 ID 幂等 + 失败可重试)
- [x] P1-1 ES 零覆盖快速失败
- [x] (条件)walk-forward 当前 `not_applicable`:现有 2 天 snapshot/零有效训练覆盖不足以组成 ≥3 fold；本片不伪造 fold，保留既有严格 60/20/20 时序切分

## Implementation

- `wbot backtest -report -push`：从 `wbot.conf` 读取 Discord bot token/channel，JSON/HTML 落盘后投影 7 字段 embed；完整 `risk` 超限即失败，不截断。
- 幂等双层保护：`report_id` 派生 25 字符 Discord `enforce_nonce`；成功响应后才在 `~/.wbot/backtest-push/discord/` 原子写 sent 标记，Unix 用 `flock` 串行并发进程。失败不写标记、exit 1，同命令可重试；成功重跑输出 `already_sent`。
- ES 在 baseOutcome probe 后要求 `valid_coverage > 0 && effective_trades > 0`，否则包装 `backtestexec.ErrNoOptionData` 快速失败；样本外候选还要求每个 seed 都有有效成交，零收益候选不能借负基线进入推荐列表。
- 新增 `scripts/accept-backtest-push.sh` 并挂入 `scripts/verify.sh` 与 CI test job；验收总表重新实计为 18 脚本/217 checks。

## Verification

- `scripts/verify.sh`：全绿（gofmt/test/vet/race/staticcheck、五平台交叉编译、CLI smoke、零依赖 accept）。
- `scripts/accept-backtest-report.sh` 11/11、`accept-backtest-train.sh` 8/8、`accept-backtest-push.sh` 13/13。
- 本地假 Discord Bot API：首发 503 → 报告保留/无 marker；同 ID 重试成功；第三次零网络请求；nonce/7 字段/完整风险/token+channel 路由均对账。
- 真实 PostgreSQL `wbot-pg-ci-test`：`TestBacktestTrainZeroCoverageIntegration` 通过，确认在 ES 种群评估前以 coverage=0/effective_trades=0 快速失败并且不生成训练报告。

## Next

开发与自测完成 → reviewer 独立评审 → 主会话决定合入 → operator 部署后用真实 Discord channel 做一次 smoke（本片未擅自向外部频道发消息）。

## 收口(2026-08-14,supervisor-fc)

- 评审:reviewer 建议合入 — feature(含 1 处 bugfix 性质 P1-1),无 P0/P1;P2 1 项(推送失败退出码分层 permanent vs transient,需产品确认,记排期);P3 观察(零成交错误措辞 backtest_train.go:246、Windows stale 锁回收 lock_windows.go:24-27、rename 后目录 fsync push.go:189、单提交粒度)
- 合入:e4f6a69(fix/backtest-p1 → feat/llm-signal-endpoint,--no-ff),已 push
- 验收:verify 全绿 + accept-backtest-push 13/13 + 真实 PG 集成测通过;未向真实 Discord 发送(留 operator 部署后 smoke)
- #64(P1-1 ES 零覆盖快速失败)随本片完成,标记 completed

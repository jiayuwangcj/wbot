# 闭环 #87: API.md `/ui/*` 静态资源枚举补 favicon.svg(文档层对账)

- **日期**: 2026-08-03
- **PR**: #291(功能)+ 本文档(归档)
- **背景**: 「同一误述整链清零」原则复查——#86 修了 webui.go 注释层枚举(4→7 文件含 favicon.svg),但 doc/API.md:17 的 `/ui/*` 行仍只列 `style.css`、`app.js`。注释层已修、文档层漏网,同类漂移的整链未清零。

## 改动

- doc/API.md:17:`/ui/*` 静态资源枚举补 `favicon.svg`(web/ 现 7 文件)
- 全库复扫:`webui.go` 注释(#86 已含)、`webui_test.go`(契约断言)、README(无枚举)——仅此一处漏网,已修

## 验证

- verify.sh 全绿;docs-only → CI 5/5(check-skip 3s + db-integration 27s)
- 与 #86 教训一致:枚举清单漂移要全库扫「同一枚举点」,不能只修发现处

## 备注

- **引擎经验(注释层对账延伸)**: 代码注释、API 文档、测试契约三处枚举独立存在,修一处必须全库 grep 同模式清零。
- **候选池**: 仍枯竭(待老板 7 项)。

# 闭环 #78: README 项目约束段去过时「后续实现」标记

- **日期**: 2026-08-03
- **PR**: #273(功能)+ 本文档(归档)
- **背景**: 约束段标记对账维度重扫——README「项目约束(已确认)」段 5 处「(后续实现)」与实际不符:港股/美股现货+期权已接入(futu 网关)、go:embed 内嵌已实现且前端实为**原生 HTML/CSS/JS 非 React**、master/agent 占位子系统已实现、富途已接入(IBKR 仍待后续)。

## 改动

- README.md 项目约束段逐条对齐:市场→「已接入(见 doc/FUTU.md)」;Web→「前端原生 HTML/CSS/JS 经 go:embed 内嵌」;架构→「占位子系统已实现(见 doc/API.md Agent federation);多机 HTTPS 部署待后续」;交易接入→「富途已接入;IBKR 抽象层待后续」;部署形态→「前台 serve(dev-up/日构建脚本以守护方式启动)」。外部通知保留「后续实现」(告警渠道 token 待老板)

## 验证

- 全文 React 零残留;「后续实现」仅剩外部通知 1 处;docs-only CI check-skip 通过

## 备注

- **引擎经验**: 约束段「(后续实现)」标记对账要**逐条对照现状**,「部分落地」的条目拆成「已实现部分 + 待后续部分」;旧表述(React)本身就是欠账——实现演进后愿景描述也要跟着对齐,不能只改标记不改描述。
- **同日运维沉淀**: vdaily-20260803 republish 重打 tag 到 HEAD(含 #267-#272 六 PR,release 落后于 HEAD)→ deploy 校验通过(~/.wbot/releases/daily-20260803,二进制实测含 #77 端点形状修正)。
- **候选池**: 仍枯竭(待老板 7 项)。

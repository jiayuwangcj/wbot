import type { ReactNode } from "react";
import { Typography } from "antd";

export function WatchlistPage(): ReactNode {
  return <div className="placeholder-panel"><Typography.Title level={2}>策略</Typography.Title><Typography.Paragraph>迁移中，切片 P1 交付。</Typography.Paragraph></div>;
}

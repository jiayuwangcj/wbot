import type { ReactNode } from "react";
import { Typography } from "antd";

export function DataPage(): ReactNode {
  return <div className="placeholder-panel"><Typography.Title level={2}>数据</Typography.Title><Typography.Paragraph>迁移中，切片 P3 交付。</Typography.Paragraph></div>;
}

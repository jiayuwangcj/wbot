import type { ReactNode } from "react";
import { Typography } from "antd";

export function AdminPage(): ReactNode {
  return <div className="placeholder-panel"><Typography.Title level={2}>Admin</Typography.Title><Typography.Paragraph>迁移中，切片 P4 交付。</Typography.Paragraph></div>;
}
